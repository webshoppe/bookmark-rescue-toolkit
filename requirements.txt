# Bookmark Rescue Toolkit — Python dependencies
# Install with:  pip install -r requirements.txt
#
# Only one external package is required.
# Everything else (sqlite3, tkinter, json, pathlib, threading …)
# ships with the Python standard library.

customtkinter>=5.2.0
